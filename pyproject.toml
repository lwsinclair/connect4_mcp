[project]
name = "connect4-mcp"
version = "0.1.0"
description = "play connect 4 with any mcp client!!!"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.6.0",
    "numpy>=2.2.4",
    "pygame>=2.6.1",
]
